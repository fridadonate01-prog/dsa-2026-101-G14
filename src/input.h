#ifndef INPUT_H
#define INPUT_H
#include <string.h>
#include <stdio.h>
int fact(int n);
const char* choose_map(const char *maps[]);
const char* choose_input(const char * all_location_inputs[]);
#endif